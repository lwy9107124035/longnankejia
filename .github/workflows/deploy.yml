name: Build & Deploy to Netlify

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate secrets.js
        run: |
          echo "window.APP_SECRETS={apiKey:\"${{ secrets.SILICONFLOW_API_KEY }}\"};" > js/secrets.js

      - name: Deploy to Netlify
        run: |
          npm install -g netlify-cli
          BRANCH="${GITHUB_REF#refs/heads/}"
          echo "Deploying branch: $BRANCH"
          # 只有 main 能自动部署。别名分支不再由 push 触发（dev 分支已删除，
          # 它的预览站会永久停在旧内容上），需要预览时用 workflow_dispatch 手动跑。
          if [ "$BRANCH" = "main" ]; then
            netlify deploy --dir . --prod \
              --auth "$NETLIFY_AUTH_TOKEN" \
              --site "$NETLIFY_SITE_ID"
          else
            netlify deploy --dir . --alias "$BRANCH" \
              --auth "$NETLIFY_AUTH_TOKEN" \
              --site "$NETLIFY_SITE_ID"
          fi
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
